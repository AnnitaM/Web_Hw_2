window.onload = init;
function init() {
    var button = document.getElementById("addButton")
    button.onclick = submit;
}
var button = getElementById("addButton");
function submit() {
    alert("You subscribed to the axalotl newsletter")
}
button.onclick = send;